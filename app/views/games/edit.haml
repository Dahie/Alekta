%h1 Editing game

-form_for(@game) do |f| 
  = f.error_messages 
  = render :partial => 'form', :locals => { :f => f}

  p
    = f.submit "Update"

= link_to 'Show', @game
|
= link_to 'Back', games_path 
