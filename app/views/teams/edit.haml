%h1 Editing team

-form_for(@team) do |f|
  = f.error_messages 
  = render :partial => 'form', :locals => {:f => f}
  %p
    = f.submit "Update" 

= link_to 'Show', @team
|
= link_to 'List all Teams', teams_path 
