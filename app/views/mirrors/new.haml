%h1 New mirror

-form_for ([@mod, @release, @mirror])  do |f|
  %fieldset{:title => "Mirror"}
    %legend Mirror
    = f.error_messages
    = render :partial => "form", :locals => {:f => f}
    %p
      = f.submit "Create Mirror"
  
  =link_to 'Back', mods_path(mod)