%h1 Editing mod

-form_for(@mod) do |f|
  = f.error_messages
  = render :partial => 'form', :locals => {:f => f}
  %p>
    = f.submit "Update mod"

= link_to 'Show', @mod
|
= link_to 'List all Mods', mods_path
